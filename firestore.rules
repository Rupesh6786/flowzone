rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read/write access to all documents.
    // This is not secure for a production app, but is fine for development.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
